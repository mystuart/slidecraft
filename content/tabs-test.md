---
title: Tabs 组件测试
subtitle: 标签页切换组件验证
author: 测试
sections:
  - 一、多解法对比
  - 二、多视角对照
  - 三、边界情况
---

# Tabs 组件测试

## 一、多解法对比

同一道题的两种解法并列查看（点击标签切换）。

```tabs
{
  "title": "求 $\\sum_{i=1}^{n} i$ 的两种方法",
  "tabs": [
    {"label": "公式法", "content": "用等差数列求和公式：$$S_n = \\frac{n(n+1)}{2}$$。直接代入，$O(1)$ 时间复杂度，最快。"},
    {"label": "循环法", "content": "逐项累加，从 1 加到 n。$O(n)$ 时间复杂度，但更直观，适合教学演示。"},
    {"label": "递归法", "content": "$f(n) = n + f(n-1)$，边界 $f(1) = 1$。$O(n)$ 时间 + $O(n)$ 栈空间，不推荐实际使用。"}
  ]
}
```

## 二、多视角对照

同一概念的不同视角。

```tabs
{
  "tabs": [
    {"label": "直觉", "content": "导数就是**变化率**——函数在某点有多陡。"},
    {"label": "形式", "content": "$f'(x) = \\lim_{h \\to 0} \\frac{f(x+h) - f(x)}{h}$"},
    {"label": "几何", "content": "导数 = 切线斜率。曲线在某点的切线越陡，导数绝对值越大。"}
  ]
}
```

## 三、边界情况

空 tabs（应报错）：

```tabs
{
  "title": "空测试",
  "tabs": []
}
```

单个 tab：

```tabs
{
  "tabs": [{"label": "唯一", "content": "只有一个标签页时也应正常显示和切换。"}]
}
```
