{
  "name": "slidecraft",
  "version": "1.3.0",
  "private": true,
  "description": "Interactive Slidecraft handouts from Markdown. 16 components · single-file · zero runtime.",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "katex": "^0.16.9",
    "marked": "^18.0.4",
    "three": "^0.184.0",
    "highlight.js": "^11.11.1"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}