{
  "name": "courseware",
  "version": "1.1.0",
  "private": true,
  "description": "交互课程课件框架 · 10 个 Markdown 内嵌组件 + 单文件 build",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "katex": "^0.16.9",
    "marked": "^18.0.4",
    "three": "^0.184.0"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
