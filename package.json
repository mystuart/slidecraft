{
  "name": "courseware",
  "version": "1.1.0",
  "private": true,
  "description": "交互课程课件框架 · 10 个 Markdown 内嵌组件 + 单文件 build",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "katex": "^0.16.9",
    "marked": "^12.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
